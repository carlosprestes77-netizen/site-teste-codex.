import { flashItems } from '@/lib/data';
export default function FlashGallery(){ return <section id="flash" className="px-5 py-24 lg:px-8 lg:py-36"><div className="mx-auto max-w-7xl"><p className="label-section">03 — Flash disponível</p><h2 className="mt-6 max-w-3xl font-serif text-5xl font-light lg:text-7xl">Desenhos prontos para uma única pele.</h2><div className="mt-14 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{flashItems.map(f=><a href={`#simulador?flash=${encodeURIComponent(f.name)}`} key={f.id} className="group border border-ink/15 bg-paper-100 p-4"><div className="grid aspect-square place-items-center bg-paper-200"><img src={f.src} alt={f.name} className="h-full w-full object-contain p-8 grayscale transition duration-700 group-hover:scale-105 group-hover:grayscale-0"/></div><div className="pt-5"><p className="font-serif text-2xl">{f.name}</p><p className="mt-1 text-[10px] uppercase tracking-[0.3em] text-ink-muted">{f.style}</p><p className="mt-3 text-sm leading-relaxed text-ink-muted">{f.description}</p></div></a>)}</div></div></section> }
