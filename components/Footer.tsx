import { Clock, Instagram, MapPin, MessageCircle } from 'lucide-react';
import { ARTIST, STUDIO_NAME } from '@/lib/data';
const nav=[['Portfólio','#portfolio'],['Manifesto','#manifesto'],['Simulador','#simulador'],['Orçamento','#orcamento']];
export default function Footer(){ return <footer className="bg-ink px-5 py-16 text-paper-100 lg:px-8"><div className="mx-auto grid max-w-7xl gap-10 md:grid-cols-4"><div><h2 className="font-serif text-3xl">{STUDIO_NAME}</h2><p className="mt-4 text-sm leading-relaxed text-paper-400">Tatuagem autoral para quem prefere carregar sentido, não tendência.</p></div><div><p className="text-[10px] uppercase tracking-[0.35em] text-paper-400">Navegação</p><div className="mt-4 grid gap-2">{nav.map(([l,h])=><a href={h} key={h} className="text-paper-300 hover:text-gold-pale">{l}</a>)}</div></div><div><p className="text-[10px] uppercase tracking-[0.35em] text-paper-400">Horários</p><div className="mt-4 grid gap-3">{ARTIST.hours.map(h=><p className="flex gap-2 text-paper-300" key={h.day}><Clock size={16}/><span>{h.day}: {h.time}</span></p>)}</div></div><div><p className="text-[10px] uppercase tracking-[0.35em] text-paper-400">Contato</p><div className="mt-4 grid gap-3 text-paper-300"><a className="flex gap-2" href={ARTIST.instagram}><Instagram size={16}/>{ARTIST.handle}</a><a className="flex gap-2" href={`https://wa.me/${ARTIST.whatsapp}`}><MessageCircle size={16}/>WhatsApp</a><p className="flex gap-2"><MapPin size={16}/>{ARTIST.city}</p></div></div></div><p className="mx-auto mt-12 max-w-7xl border-t border-paper-100/10 pt-6 text-xs text-paper-500">© 2026 {STUDIO_NAME}. Todos os direitos reservados.</p></footer> }
