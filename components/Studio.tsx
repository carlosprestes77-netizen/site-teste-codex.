import { Instagram, ShieldCheck, Sparkles, Waves } from 'lucide-react';
import { ARTIST } from '@/lib/data';
export default function Studio(){ const vals=[['Autoria','Nenhum desenho sai igual ao outro.',Sparkles],['Segurança','Materiais certificados e rotina rigorosa.',ShieldCheck],['Fluxo','Composição pensada para movimento.',Waves]] as const; return <section id="estudio" className="px-5 py-24 lg:px-8 lg:py-36"><div className="mx-auto grid max-w-7xl gap-12 lg:grid-cols-2 lg:items-center"><div className="relative border border-ink/15 p-4"><img src={ARTIST.portrait} alt={`Retrato de ${ARTIST.name}`} className="aspect-[4/5] w-full object-cover grayscale"/><a href={ARTIST.instagram} className="absolute bottom-8 right-8 flex items-center gap-2 bg-paper-100 px-4 py-3 text-[10px] uppercase tracking-[0.25em]"><Instagram size={15}/>{ARTIST.handle}</a></div><div><p className="label-section">06 — Estúdio</p><h2 className="mt-6 font-serif text-5xl font-light lg:text-7xl">Precisão editorial, acolhimento real.</h2><p className="mt-8 text-lg leading-relaxed text-ink-muted">{ARTIST.name} cria tatuagens autorais em {ARTIST.city}, combinando desenho refinado, leitura anatômica e uma experiência silenciosa o bastante para você ouvir a própria decisão.</p><div className="mt-10 grid gap-5">{vals.map(([t,d,Icon])=><div className="flex gap-4 border-t border-ink/15 pt-5" key={t}><Icon className="text-gold"/><div><h3 className="font-serif text-2xl">{t}</h3><p className="text-ink-muted">{d}</p></div></div>)}</div></div></div></section> }
